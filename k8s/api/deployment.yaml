apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: chatbot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: chatbot/api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: DATABASE_URL
            - name: SYNC_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: SYNC_DATABASE_URL
            - name: REDIS_URL
              value: redis://redis:6379
            - name: PRESIDIO_URL
              value: http://presidio:8080
            - name: ENVIRONMENT
              value: production
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-api-keys
                  key: ANTHROPIC_API_KEY
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-api-keys
                  key: OPENAI_API_KEY
            - name: GOOGLE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-api-keys
                  key: GOOGLE_API_KEY
            - name: SARVAM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-api-keys
                  key: SARVAM_API_KEY
            - name: GROQ_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-api-keys
                  key: GROQ_API_KEY
          # startupProbe gates liveness/readiness until migrations finish
          # 120 attempts × 5s = 10 min window for alembic upgrade head
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            failureThreshold: 120
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
